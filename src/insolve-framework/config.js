import { API_BASE_URL, AUTH_URL } from '../constants';

export default {
  // NOTE: Change this to correct server URL (example: https://dev.rocketbets.gg:443)
  url: API_BASE_URL,
  authUrl: AUTH_URL,
  // NOTE: Enabling authUrl -> enables external auth server redirect
  name: 'rockethub',
  title: 'Onboard a rocket to riches!', // in browser

};
